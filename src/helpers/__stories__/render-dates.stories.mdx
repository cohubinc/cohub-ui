import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";
import { renderDate, StoryHelpers, Typography } from "dist/index.esm.js";
import { formats } from "../render-dates";

<Meta title="Components|renderDate" />

# RenderDate

## A function for formating ISO 8601 dates

<Story name="renderDate">
  <div style={{ width: 400 }}>
    <div className="flex justify-between">
      <Typography.HeadingTiny bold>Argument</Typography.HeadingTiny>
      <Typography.HeadingTiny bold>Output</Typography.HeadingTiny>
    </div>
    {formats.map(format => {
      return (
        <div key={format} className="flex justify-between mt-1 border-b">
          <Typography.Large bold>{format}</Typography.Large>{" "}
          <Typography.Large>
            {renderDate(format)("2019-03-08T22:16:08Z")}
          </Typography.Large>
        </div>
      );
    })}
  </div>
</Story>

Example:
`<span>{renderDate("monthDayYear")("2019-03-08T22:16:08Z")}</span>`

<Props of={renderDate} />
