import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";

import { AnimatedCheckmark, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|AnimatedCheckmarks" component={AnimatedCheckmark} />

# AnimatedCheckmark

<Preview>
  <Story name="AnimatedCheckmark">
    <StoryHelpers.StateCtrl>
      {({ state }) => (
        <div
          className="flex justify-center items-center"
          style={{
            height: "8em",
            width: "14.6em"
          }}
        >
          {state && <AnimatedCheckmark size="3em" />}
        </div>
      )}
    </StoryHelpers.StateCtrl>
  </Story>
</Preview>

<Props of={AnimatedCheckmark} />
