import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";

import { AnimatedCheckmark, Color, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|AnimatedCheckmarks" />

# AnimatedCheckmark

<Story name="AnimatedCheckmark">
  <div>
    <StoryHelpers.StateCtrl>
      {({ state }) => (
        <div
          className="flex justify-center items-center"
          style={{
            height: "8em",
            width: "14.6em",
            backgroundColor: Color.black
          }}
        >
          {state && <AnimatedCheckmark size="3em" />}
        </div>
      )}
    </StoryHelpers.StateCtrl>
    <StoryHelpers.Margin />
    <StoryHelpers.StateCtrl>
      {({ state }) => (
        <div
          className="flex justify-center items-center"
          style={{
            height: "8em",
            width: "14.6em",
            backgroundColor: Color.primary
          }}
        >
          {state && <AnimatedCheckmark size="3em" />}
        </div>
      )}
    </StoryHelpers.StateCtrl>
  </div>
</Story>

## Example usage

```jsx
<AnimatedCheckmark size="3em" />
```

<Props of={AnimatedCheckmark} />
