import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|TextArea" component={Inputs.TextArea} />

# TextArea Input

<Preview>
  <Story name="TextArea">
    <StoryHelpers.StateContainer defaultState="">
      {({ state, setState }) => (
        <Inputs.TextArea
          label="TextArea Input"
          input={{
            value: state,
            onChange: ({ target }) => setState(target.value)
          }}
          style={{ width: 300 }}
        />
      )}
    </StoryHelpers.StateContainer>
  </Story>
</Preview>

<Props of={Inputs.TextArea} />
