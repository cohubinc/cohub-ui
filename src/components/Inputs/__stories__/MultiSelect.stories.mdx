import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";
import { Form, Field } from "react-final-form";
import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|MultiSelect" />

# MultiSelect Input

<StoryHelpers.Margin />

<Story name="MultiSelect">
  <div className="mb-3">
    <Form
      initialValues={{ multiSelect: ["option1", "option2"] }}
      onSubmit={() => {}}
      render={() => (
        <Field
          name="multiSelect"
          render={props => (
            <Inputs.MultiSelect
              label="Select an option"
              allowCreate
              required
              options={[
                { label: "Option 1", value: "option1" },
                { label: "Option 2", value: "option2" },
                { label: "Option 3", value: "option3" },
                { label: "Option 4", value: "option4" }
              ]}
              {...props}
            />
          )}
        />
      )}
    />
  </div>
</Story>

## Usage

```jsx
<Inputs.MultiSelect
  label="Select an option"
  options={[
    { label: "Option 1", value: "option1" },
    { label: "Option 2", value: "option2" },
    { label: "Option 3", value: "option3" },
    { label: "Option 4", value: "option4" }
  ]}
  {...props}
/>
```

<Props of={Inputs.MultiSelect} />
