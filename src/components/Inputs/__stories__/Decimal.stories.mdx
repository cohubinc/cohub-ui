import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";

import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|Decimal" />

# Decimal Input

<StoryHelpers.Margin />

<Story name="Decimal">
  <div>
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <div className="mt-1" style={{ maxWidth: 300 }}>
          <StoryHelpers.StateContainer defaultState="">
            {({ state, setState }) => (
              <Inputs.Decimal
                label="Decimal Input"
                input={{
                  value: state,
                  onChange: value => setState(value)
                }}
                meta={{ touched: true, error }}
              />
            )}
          </StoryHelpers.StateContainer>
        </div>
      )}
    </StoryHelpers.StateCtrl>
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <div className="mt-1" style={{ maxWidth: 300 }}>
          <StoryHelpers.StateContainer defaultState="">
            {({ state, setState }) => (
              <Inputs.Decimal
                appearance="contrast"
                label="Contrast Decimal Input"
                textAlign="center"
                input={{
                  value: state,
                  onChange: value => setState(value)
                }}
                meta={{ touched: true, error }}
              />
            )}
          </StoryHelpers.StateContainer>
        </div>
      )}
    </StoryHelpers.StateCtrl>
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <div className="mt-1" style={{ maxWidth: 300 }}>
          <StoryHelpers.StateContainer defaultState="">
            {({ state, setState }) => (
              <Inputs.Decimal
                appearance="inverted"
                label="Inverted Decimal Input"
                textAlign="right"
                input={{
                  value: state,
                  onChange: value => setState(value)
                }}
                meta={{ touched: true, error }}
              />
            )}
          </StoryHelpers.StateContainer>
        </div>
      )}
    </StoryHelpers.StateCtrl>
  </div>
</Story>

## Usage

````jsx
<Inputs.Decimal
  label="Decimal Input"
  input={{
    value: state,
    onChange: value => setState(value)
  }}
  meta={{ touched: true, error }}
/>
```

<Props of={Inputs.Decimal} />
````
