import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|Password" component={Inputs.Password} />

# Password Input

<Preview>
  <Story name="Password">
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <StoryHelpers.StateContainer defaultState="">
          {({ state, setState }) => (
            <Inputs.Password
              label="Password Input"
              input={{
                value: state,
                onChange: ({ target }) => setState(target.value)
              }}
              meta={{ touched: true, error }}
              style={{ width: 300 }}
            />
          )}
        </StoryHelpers.StateContainer>
      )}
    </StoryHelpers.StateCtrl>
  </Story>
</Preview>

<Props of={Inputs.Password} />
