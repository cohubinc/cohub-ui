import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";

import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|Text" />

# Text Input

<StoryHelpers.Margin />

<Story name="Text">
  <div>
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <StoryHelpers.StateContainer defaultState="">
          {({ state, setState }) => (
            <div>
              <StoryHelpers.Text>Plain input</StoryHelpers.Text>
              <Inputs.Text
                label="Text Input"
                input={{
                  value: state,
                  onChange: ({ target }) => setState(target.value)
                }}
                meta={{ touched: true, error }}
                style={{ width: 300 }}
                required
              />
            </div>
          )}
        </StoryHelpers.StateContainer>
      )}
    </StoryHelpers.StateCtrl>
    <StoryHelpers.Margin />
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <StoryHelpers.StateContainer defaultState="">
          {({ state, setState }) => (
            <div>
              <StoryHelpers.Text>Contrast Input</StoryHelpers.Text>
              <Inputs.Text
                label="Contrast Text Input"
                appearance="contrast"
                input={{
                  value: state,
                  onChange: ({ target }) => setState(target.value)
                }}
                meta={{ touched: true, error }}
                style={{ width: 300 }}
                className="mt-1"
                required
              />
            </div>
          )}
        </StoryHelpers.StateContainer>
      )}
    </StoryHelpers.StateCtrl>
    <StoryHelpers.Margin />
    <StoryHelpers.StateCtrl label="Error State">
      {({ state: error }) => (
        <StoryHelpers.StateContainer defaultState="">
          {({ state, setState }) => (
            <div>
              <StoryHelpers.Text>Inverted Inputs</StoryHelpers.Text>
              <Inputs.Text
                label="Inverted Text Input"
                appearance="inverted"
                input={{
                  value: state,
                  onChange: ({ target }) => setState(target.value)
                }}
                meta={{ touched: true, error }}
                style={{ width: 300 }}
                required
              />
              <StoryHelpers.Margin />
              <StoryHelpers.Text>Inverted Input</StoryHelpers.Text>
              <Inputs.Text
                placeholder="Placeholder Text"
                appearance="inverted"
                input={{
                  value: state,
                  onChange: ({ target }) => setState(target.value)
                }}
                meta={{ touched: true, error }}
                style={{ width: 300 }}
                required
              />
            </div>
          )}
        </StoryHelpers.StateContainer>
      )}
    </StoryHelpers.StateCtrl>
  </div>
</Story>

## Usage

```jsx
<Inputs.Text
  label="Text Input"
  input={{
    value: state,
    onChange: ({ target }) => setState(target.value)
  }}
  meta={{ touched: true, error }}
  style={{ width: 300 }}
  required
/>
```

<Props of={Inputs.Text} />
