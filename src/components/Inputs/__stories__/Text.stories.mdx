import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";

import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|Text" />

# Text Input

<StoryHelpers.Margin />

<Story name="Text">
  <StoryHelpers.StateCtrl label="Error State">
    {({ state: error }) => (
      <StoryHelpers.StateContainer initialState="">
        {({ state, setState }) => (
          <Inputs.Text
            label="Text Input"
            input={{
              value: state,
              onChange: ({ target }) => setState(target.value)
            }}
            meta={{ touched: true, error }}
            style={{ width: 300 }}
          />
        )}
      </StoryHelpers.StateContainer>
    )}
  </StoryHelpers.StateCtrl>
</Story>

<Props of={Inputs.Text} />
