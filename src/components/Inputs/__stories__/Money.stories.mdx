import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";

import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|Money" />

# Money Input

<StoryHelpers.Margin />

<Story name="Money">
  <StoryHelpers.StateContainer defaultState="">
    {({ state, setState }) => (
      <div>
        <MoneyInput
          appearance="contrast"
          label="Amount"
          input={{ state, onChange: ({ target }) => setState(target.value) }}
        />
      </div>
    )}
  </StoryHelpers.StateContainer>
</Story>

<Props of={Inputs.Money} />
