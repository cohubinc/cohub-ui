import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Inputs, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Inputs|Money" component={Inputs.Money} />

# Money Input

<Preview>
  <Story name="Money">
    <StoryHelpers.StateContainer defaultState={22.67}>
      {({ state, setState }) => (
        <div>
          <Inputs.Money
            clearable
            appearance="contrast"
            label="Amount"
            input={{
              value: state,
              onChange: value => setState(value)
            }}
          />
        </div>
      )}
    </StoryHelpers.StateContainer>
  </Story>
</Preview>

<Props of={Inputs.Money} />
