---
name: Inputs
---

import { Playground, PropsTable } from "docz";
import Inputs from "src/components/Inputs";
import { Form, Field } from "react-final-form";

<PropsTable of={Inputs.Base} />

# Inputs

## Base

<Playground>
  <div className="mb-3">
    <Inputs.Base label="Base Input" />
  </div>
  <div className="mb-3 p-1" style={{ backgroundColor: "var(--dark-black)" }}>
    <Inputs.Base appearance="inverted" label="Inverted Input" />
  </div>
  <div className="mb-3">
    <Inputs.Base appearance="contrast" label="Contrast Input" />
  </div>
</Playground>

## Text

<Playground>
  <div className="mb-3">
    <Inputs.Text label="Text Input" />
  </div>
</Playground>

## Text Area

<Playground>
  <div className="mb-3">
    <Inputs.TextArea label="Text Area Input" />
  </div>
</Playground>

## Checkbox

<Playground>
  <div className="mb-3">
    <Form
      onSubmit={() => {}}
      render={() => (
        <Field
          name="checkbox"
          render={props => <Inputs.Checkbox label="Checkbox" {...props} />}
        />
      )}
    />
  </div>
</Playground>

## Select

<Playground>
  <div className="mb-3">
    <Form
      initialValues={{ select: "option1" }}
      onSubmit={() => {}}
      render={() => (
        <Field
          name="select"
          render={props => (
            <Inputs.Select
              label="Select an option"
              {...props}
              options={[
                { label: "Option 1", value: "option1" },
                { label: "Option 2", value: "option2" },
                { label: "Option 3", value: "option3" },
                { label: "Option 4", value: "option4" }
              ]}
            />
          )}
        />
      )}
    />
  </div>
</Playground>

## Multi-Select

<Playground>
  <div className="mb-3">
    <Form
      initialValues={{ multiSelect: ["option1", "option2"] }}
      onSubmit={() => {}}
      render={() => (
        <Field
          name="multiSelect"
          render={props => (
            <Inputs.MultiSelect
              label="Select an option"
              options={[
                { label: "Option 1", value: "option1" },
                { label: "Option 2", value: "option2" },
                { label: "Option 3", value: "option3" },
                { label: "Option 4", value: "option4" }
              ]}
              {...props}
            />
          )}
        />
      )}
    />
  </div>
</Playground>

## MoneyInput

<Playground>
  <Inputs.Money
    label="Amount"
    extendedPrecision={true}
    input={{
      value: 22.38778,
      onChange: moneyInputValue => console.log({ moneyInputValue })
    }}
  />
</Playground>

## Toggle

<Playground>
  <Form
    initialValues={{ toggle: true }}
    onSubmit={() => {}}
    render={() => (
      <Field
        name="toggle"
        render={props => <Inputs.Toggle label="Toggle" {...props} />}
      />
    )}
  />
</Playground>

## DateTime

<Playground>
  <Form
    initialValues={{ toggle: true }}
    onSubmit={() => {}}
    render={() => (
      <React.Fragment>
        <Field
          name="date"
          render={props => <Inputs.DateTime label="Date" {...props} />}
        />
        <Field
          name="datetime"
          render={props => (
            <Inputs.DateTime includeTime label="DateTime" {...props} />
          )}
        />
      </React.Fragment>
    )}
  />
</Playground>

## DateTimeRange

<Playground>
  <Form
    initialValues={{ toggle: true }}
    onSubmit={() => {}}
    render={() => (
      <React.Fragment>
        <Field
          name="daterange"
          render={props => (
            <Inputs.DateTimeRange label="Date Ragne" {...props} />
          )}
        />
        <Field
          name="datetimerange"
          render={props => (
            <Inputs.DateTimeRange
              includeTime
              label="DateTime Ranger"
              {...props}
            />
          )}
        />
      </React.Fragment>
    )}
  />
</Playground>
