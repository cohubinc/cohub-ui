import { Story, Meta, Props } from "@storybook/addon-docs/blocks";
import { Chip, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Chips" component={Chip} />

# Chip

<Preview>
  <Story name="Chip">
    <div>
      <Chip className="mr-05">Text Is</Chip>
      <StoryHelpers.Margin />
      <StoryHelpers.StateContainer defaultState="">
        {({ state, setState }) => (
          <Chip.Add
            name="tag"
            value={state}
            onChange={setState}
            onBlur={() => setState("")}
          />
        )}
      </StoryHelpers.StateContainer>
    </div>
  </Story>
</Preview>

<Props of={Chip} />
