---
name: Modal
---

import { Playground, PropsTable } from "docz";
import DoczStateCtrl from "src/components/Docz/DoczStateCtrl";
import Modal from "../index.tsx";

# Modal

<PropsTable of={Modal} />

## Usage

<Playground>
   <DoczStateCtrl>
    {({ on, toggleOff }) => {
      return <Modal open={on} onClose={toggleOff}>
          <div>
            <div className="text-center">Close by</div>

            <ul style={{listStylePosition: "inside", marginTop: 0}}>
              <li>clicking the backdrop</li>
              <li>clicking the close button</li>
              <li>clicking the close icon</li>
              <li>or hitting esc on your keyboard</li>
            </ul>

            <button className="w-100" onClick={toggleOff}>Close</button>
          </div>
        </Modal>
    }}

  </DoczStateCtrl>

</Playground>
