import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Modal, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Modals" component={Modal} />

# Modal

<Preview>
  <Story name="Modal">
    <div>
      <StoryHelpers.StateCtrl>
        {({ state, toggleOff }) => {
          return (
            <Modal open={state} onClose={toggleOff}>
              <div>
                <div className="text-center">Close by</div>
                <ul style={{ listStylePosition: "inside", marginTop: 0 }}>
                  <li>clicking the backdrop</li>
                  <li>clicking the close button</li>
                  <li>clicking the close icon</li>
                  <li>or hitting esc on your keyboard</li>
                </ul>
                <button className="w-100" onClick={toggleOff}>
                  Close
                </button>
              </div>
            </Modal>
          );
        }}
      </StoryHelpers.StateCtrl>
    </div>
  </Story>
</Preview>

<Props of={Modal} />
