import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";

import { Buttons, StoryHelpers, Color, Loader } from "dist/index.esm.js";

<Meta title="Components|Loaders" />

# Loader

<StoryHelpers.Margin />

## A loader

<Story name="Loader">
  <div>
    <div>
      <StoryHelpers.StateCtrl label="Loader">
        {({ state }) => <Loader show={state} />}
      </StoryHelpers.StateCtrl>
    </div>
    <StoryHelpers.Margin />
    <div>
      <StoryHelpers.StateCtrl label="Fullscreen Loader">
        {({ state }) => <Loader fullScreen show={state} />}
      </StoryHelpers.StateCtrl>
    </div>
  </div>
</Story>

<Props of={Loader} />
