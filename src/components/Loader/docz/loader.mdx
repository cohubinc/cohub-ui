---
name: Loader
---

import { Playground, PropsTable } from "docz";
import DoczStateCtrl from "src/components/Docz/DoczStateCtrl";
import Loader from "../index.tsx";

# Loader

<PropsTable of={Loader} />

## Description

Cohub branded loader

## Usage

<Playground>
  <DoczStateCtrl>{({ on, toggleOff }) => <Loader show={on} />}</DoczStateCtrl>
</Playground>

### Full screen example

<Playground>
  <DoczStateCtrl>
    {({ on, toggleOff }) => <Loader fullScreen show={on} />}
  </DoczStateCtrl>
</Playground>
