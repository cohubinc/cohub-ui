import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Backdrop, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|Backdrop" component={Backdrop} />

# Backdrop

<Preview>
  <Story name="Backdrop">
    <StoryHelpers.StateCtrl label="Backdrop">
      {({ state, toggleOff }) => (
        <Backdrop open={state} onClose={toggleOff}>
          <div style={{ color: "white" }}>
            <div className="text-center">Close by</div>
            <ul style={{ listStylePosition: "inside", marginTop: 0 }}>
              <li>clicking the backdrop</li>
              <li>clicking the button</li>
              <li>or hitting esc on your keyboard</li>
            </ul>
            <button className="w-100" onClick={toggleOff}>
              Close
            </button>
          </div>
        </Backdrop>
      )}
    </StoryHelpers.StateCtrl>
  </Story>
</Preview>

<Props of={Backdrop} />
