import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { FormGroup, StoryHelpers, Color, Inputs } from "dist/index.esm.js";

<Meta title="Components|FormGroups" component={FormGroup} />

# FormGroup

## A way to group sections of a form

<Preview>
  <Story name="FormGroup">
    <div>
      <StoryHelpers.StateCtrl label="Direction" defaultState={true}>
        {({ state }) => {
          return (
            <FormGroup direction={state ? "horizontal" : "vertical"}>
              <Inputs.Text label="First Name" />
              <Inputs.Text label="Last Name" />
            </FormGroup>
          );
        }}
      </StoryHelpers.StateCtrl>
    </div>
  </Story>
</Preview>

<Props of={FormGroup} />
