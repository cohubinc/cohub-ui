import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";

import { Buttons, StoryHelpers } from "../../../../dist/index.storybook.js";

<Meta title="Components|Buttons|Cancel" />

# Cancel Button

<Story name="Cancel">
  <div>
    <Buttons.Cancel>Cancel</Buttons.Cancel>
    <StoryHelpers.Margin />
    <StoryHelpers.StateCtrl label="raised" defaultState={true}>
      {({ state }) => <Buttons.Cancel raised={state}>Raised</Buttons.Cancel>}
    </StoryHelpers.StateCtrl>
  </div>
</Story>

<StoryHelpers.Margin showDividerLine marginBottom={0} />

Cancel only excepts the **raised** and **backgroundColor** props

<Props of={Buttons.Base} />

**Also accepts all [native button attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#Attributes) as props**
