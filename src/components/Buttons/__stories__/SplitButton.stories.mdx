import { Story, Meta, Props, Source } from "@storybook/addon-docs/blocks";

import { SplitButton, StoryHelpers } from "../../../../dist/index.js";

<Meta title="Components|Buttons|SplitButton" />

# SplitButtons

## Usage

<Story name="SplitButtons">
  <div>
    <Text underlined>Default Split Buttons</Text>
    <StoryHelpers.StateContainer initialState={0}>
      {({ setState, state }) => (
        <Split
          labels={["Brown", "Chicken", "BrownCow"]}
          selectedIndex={state}
          onChange={setState}
        />
      )}
    </StoryHelpers.StateContainer>
    <StoryHelpers.Margin />
    <StoryHelpers.Text underlined>Primary Split Buttons</StoryHelpers.Text>
    <StoryHelpers.Text italicize>
      Segments will always be the same size
    </StoryHelpers.Text>
    <StoryHelpers.StateContainer initialState={1}>
      {({ setState, state }) => (
        <Split.Primary
          labels={["Collections", "Search", "Select an item at random"]}
          selectedIndex={state}
          onChange={setState}
        />
      )}
    </StoryHelpers.StateContainer>
  </div>
</Story>

<Props of={Split} />
