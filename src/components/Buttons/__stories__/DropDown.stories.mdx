import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Buttons, StoryHelpers, Color } from "dist/index.esm.js";

<Meta title="Components|Buttons|Dropdown" component={Buttons.Dropdown} />

# Outline Button

<Preview>
  <Story name="Dropdown">
    <div style={{ width: "13rem" }}>
      <Buttons.Dropdown
        buttonType="Primary"
        options={[
          {
            onClick: () => console.log("Primary click"),
            label: "Primary"
          },
          {
            onClick: () => console.log("Option two click"),
            label: "Option Two"
          },
          {
            onClick: () => console.log("Option three click"),
            label: "Option Three"
          },
          {
            onClick: () => console.log("Option four click"),
            label: "Option Four"
          }
        ]}
      />
      <StoryHelpers.Margin />
      <Buttons.Dropdown
        buttonType="Info"
        options={[
          {
            onClick: () => console.log("Info click"),
            label: "Info"
          },
          {
            onClick: () => console.log("Bar click"),
            label: "Bar"
          }
        ]}
      />
      <StoryHelpers.Margin />
      <Buttons.Dropdown
        buttonType="Cancel"
        options={[
          {
            onClick: () => console.log("Cancel click"),
            label: "Cancel"
          },
          {
            onClick: () => console.log("Right click"),
            label: "Right"
          }
        ]}
      />
      <StoryHelpers.Margin />
      <Buttons.Dropdown
        options={[
          {
            onClick: () => console.log("Default click"),
            label: "Default"
          },
          {
            onClick: () => console.log("Down click"),
            label: "Down"
          }
        ]}
      />
    </div>
  </Story>
</Preview>

<Props of={Buttons.Dropdown} />
