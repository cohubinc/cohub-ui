import { Story, Meta, Props, Source } from '@storybook/addon-docs/blocks';

import Buttons from '../../Buttons/index';
import Base from '../../Buttons/Base';
import { Margin, StateCtrl } from '../../story-helpers';

<Meta title="Components|Buttons" />

# Primary Button

<Margin />

<Story name="Primary">
  <div>
    <Buttons.Primary>Primary</Buttons.Primary>
    <Margin />
    <StateCtrl label="error">
      {({ state }) => <Buttons.Primary error={state}>Error</Buttons.Primary>}
    </StateCtrl>
    <Margin />
    <StateCtrl label="raised" defaultState>
      {({ state }) => <Buttons.Primary raised={state}>Raised</Buttons.Primary>}
    </StateCtrl>
    <Margin />
    <StateCtrl label="success">
      {({ state }) => (
        <Buttons.Primary success={state} style={{ width: '5rem' }}>
          Submit
        </Buttons.Primary>
      )}
    </StateCtrl>
  </div>
</Story>

<Props of={Base} />

**Also accepts all [native button attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#Attributes) as props**
