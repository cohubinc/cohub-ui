import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { Tooltip } from "dist/index.esm.js";

<Meta title="Components|Tooltips" component={Tooltip} />

# Tooltip

<Preview>
  <Story name="Tooltip">
    <div>
      <Tooltip
        content="Tooltip Content"
        placement="left-end"
        theme="dark"
        arrow={false}
        duration={200}
        delay={[100, 300]}
        interactive={true}
      >
        <button>Hover Me</button>
      </Tooltip>
    </div>
  </Story>
</Preview>

<Props of={Tooltip} />
