import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { StoryHelpers, Tabs, Segment, Typography } from "dist/index.esm.js";

<Meta title="Components|Tabs" component={Tabs} />

# Tabs

These are Tabs.

<Preview>
  <Story name="Tabs">
    <div style={{ height: 200 }}>
      <StoryHelpers.StateContainer defaultState={0}>
        {({ state, setState }) => {
          const Container = ({ children, ...props }) => (
            <Segment className="flex justify-center items-center" {...props}>
              <h1>{children}</h1>
            </Segment>
          );
          return (
            <Tabs useRedux={false}>
              <Tabs.Tab
                title="Tab 1"
                onClick={() => setState(0)}
                active={state === 0}
                component={<Container>One</Container>}
              />
              <Tabs.Tab
                title="Tab 2"
                onClick={() => setState(1)}
                active={state === 1}
                component={<Container>Two</Container>}
              />
              <Tabs.Tab
                title="Tab 3"
                onClick={() => setState(2)}
                active={state === 2}
                component={<Container>Three</Container>}
              />
            </Tabs>
          );
        }}
      </StoryHelpers.StateContainer>
    </div>
  </Story>
</Preview>

## More Examples

```jsx
<Typography.Large>Route based Tabs example</Typography.Large>
<Tabs>
  <Tab
    title="Essentials"
    path="product/22"
    component={<EssentialsTab />}
  />
  <Tab
    title="Inventory Flow"
    path="product/22/inventory"
    component={<Segment>...</Segment>}
  />
  <Tab
    title="Analytics"
    path="product/22/analytics"
    component={<Segment>Charts and stuff</Segment>}
  />
  <Tab
    title="Website"
    path={Urls.productWebsitePath(product.id)}
    component={<Segment>Charts and stuff</Segment>}
  />
</Tabs>
<Typography.Large>Tabs as controlled compoents example</Typography.Large>
<Tabs useRedux={false}>
  <Tabs.Tab
    title="Tab 1"
    onClick={() => setState(0)}
    active={state === 0}
    component={<Container>One</Container>}
  />
  <Tabs.Tab
    title="Tab 2"
    onClick={() => setState(1)}
    active={state === 1}
    component={<Container>Two</Container>}
  />
  <Tabs.Tab
    title="Tab 3"
    onClick={() => setState(2)}
    active={state === 2}
    component={<Container>Three</Container>}
  />
</Tabs>
```

## Tabs Props

<Props of={Tabs} />

## Tab Props

<Props of={Tabs.Tab} />
