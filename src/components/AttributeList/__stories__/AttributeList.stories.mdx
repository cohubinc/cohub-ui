import { Story, Meta, Props, Preview } from "@storybook/addon-docs/blocks";
import { AttributeList, StoryHelpers } from "dist/index.esm.js";

<Meta title="Components|AttributeList" component={AttributeList} />

# Attribute List

<Preview>
  <Story name="AttributeList">
    <div>
      <AttributeList
        header="Money"
        contrast
        items={[
          { attribute: "Price", value: 100, format: "money" },
          { attribute: "Cost", value: 10, format: "money" }
        ]}
      />
      <StoryHelpers.Margin />
      <AttributeList
        header="Other"
        contrast
        maxVisible={2}
        expandable={true}
        items={[
          { attribute: "Price", value: 100, format: "money" },
          { attribute: "Cost", value: 10, format: "money" },
          { attribute: "Net Cost", value: 20, format: "money" },
          { attribute: "Margin", value: 10, format: "percentage" }
        ]}
      />
    </div>
  </Story>
</Preview>

<Props of={AttributeList} />
